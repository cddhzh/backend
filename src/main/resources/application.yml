spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mima?serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: 200009
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  profiles:
    active: dev #上线改为服务器配置，即prod
server:
  port: 8181